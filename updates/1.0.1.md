# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Fixed attached prop cleanup when multiple entries of the same type exist.
2. Updated `ClearPlayerPedAttachedPropsByType()` to avoid modifying `AttachedProps` while iterating through it.
3. All matching entities are now removed before rebuilding the `AttachedProps` table.
4. Prevents stale attached props and incorrect attachment state after repeated attach/detach events.


## Modified Files:

- client/tp-client_main.lua
- fxmanifest.lua
